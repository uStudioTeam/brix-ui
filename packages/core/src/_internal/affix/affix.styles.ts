import styled from 'styled-components';

const Affix = styled.span`
  &:first-child {
    margin-right: 8px;
  }

  &:last-child {
    margin-left: 8px;
  }
`;

const Styled = { Affix };

export default Styled;
